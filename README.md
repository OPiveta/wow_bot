recognize the skill in a specific region of the monitor and send a command to the Arduino; the Arduino returns the command and presses the desired key,
create a folder named: img/ and put the desired PNG images there, the code is very intuitive, in the folder there is a txt with the libraries you must have.
ty.

